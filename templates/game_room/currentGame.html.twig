{% extends 'base.html.twig' %}

{% block title %}Hello GameRoomController!{% endblock %}

{% block body %}
    <style>
        .example-wrapper {
            margin: 1em auto;
            max-width: 800px;
            width: 95%;
            font: 18px/1.5 sans-serif;
        }

        .example-wrapper code {
            background: #F5F5F5;
            padding: 2px 6px;
        }
    </style>

    <div class="example-wrapper">

    <a href="{{ path('app_game_room') }}">Retourner à l'accueil</a>

    <div {{ react_component('CurrentGameRoom', {
        'users': users,
        'skullkingid': skullkingid,
        'pathEnterGameRoom': path("enter_in_game", { id: roomid}),
        'pathCurrentGame' :  skullkingid is null ? null : path('current_game', {id : skullkingid}),
        'eventSourceUrl': mercure(topicName, { subscribe: topicName })
    }) }}>
        Loading... <i class="fas fa-cog fa-spin fa-3x"></i>
    </div>

{% endblock %}